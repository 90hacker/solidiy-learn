{
  "name": "common-token",
  "version": "1.0.0",
  "description": "Hardhat project for common-token contracts (Solidity 0.8.31)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.4"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.11.0"
  }
}
