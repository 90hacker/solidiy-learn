Hardhat project scaffold for the common-token contracts.

Key points:
- Solidity compiler set to 0.8.31 in `hardhat.config.js`.
- Contracts are in `contracts/`.
- Example deploy script in `scripts/deploy.js` (adjust constructor args before use).

Quick start:
1. npm install
2. npx hardhat compile
